const swiper = new Swiper('.swiper', {
    // Optional parameters
    loop: true,
  
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },
  
    // Navigation arrows
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  
    // And if we need scrollbar
    scrollbar: {
      el: '.swiper-scrollbar',
    },
  });


  const swiper1 = new Swiper('.hyper', {
    // Optional parameters
    loop: true,
  
    // If we need pagination
    pagination: {
      el: '.hyper-pagination',
    },
  
    // Navigation arrows
    navigation: {
      nextEl: '.hyper-button-next',
      prevEl: '.hyper-button-prev',
    },
  
    // And if we need scrollbar
    scrollbar: {
      el: '.hyper-scrollbar',
    },
  });